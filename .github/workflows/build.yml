BigBang3D/
├── source/
│   └── main.c
├── gfx/
│   ├── icon.png        (48x48 PNG)
│   └── banner.png      (256x128 PNG)
├── audio/
│   └── banner.wav      (16-bit PCM WAV)
├── Makefile
└── .github/workflows/build.yml
name: Build Big Bang 3D CIA

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

TARGET := BigBang3D
BUILD := build
SOURCES := source

APP_TITLE := Big Bang 3D
APP_DESCRIPTION := Neon arcade explosion arcade
APP_AUTHOR := YourName

ICON := gfx/icon.png
BANNER := gfx/banner.png
AUDIO := audio/banner.wav

LIBS := -lctru -lcitro2d -lcitro3d -lm

include $(DEVKITPRO)/libctru/base_rules
        
        
